# arb-core

Ядро спортивного **арбитража**: сканирует линии букмекеров, находит **вилки
(surebet)** — комбинации коэффициентов, где ставка на все исходы даёт
гарантированную прибыль независимо от результата, — и обогащает их **ИИ-разбором
матча**: кому нужнее победа (турнирная мотивация) и какой исход вероятнее.

> Отдельный модуль внутри репозитория, не связан с WhatsApp-ботом SPECTO.

## Как это работает

```
The Odds API ──► normalize ──► arbitrage ──► scanner ──► вилки + ИИ-разбор
(коэффициенты)   лучший коэф.   Σ(1/odds)<1               (ранжировано)
                 на исход                       ▲
API-Football ───────────────────────────────────┘
(форма/таблица)          motivation (Claude): кому нужна победа, вероятный исход
```

1. **normalize** — из линий всех БК берёт лучший коэффициент по каждому исходу.
   Рынки: `h2h` (1X2), `totals` (тоталы) и `spreads` (форы). Для тоталов/фор
   арбитраж считается строго в пределах одной линии (Тотал 2.5 ≠ Тотал 3.0;
   форы — зеркальная пара хозяева @−h / гости @+h).
2. **arbitrage** — если `Σ(1/odds) < 1`, это вилка; считает раскладку ставок с
   равной выплатой и гарантированную маржу/ROI.
3. **motivation** — Claude оценивает «кому нужнее победа» и вероятности исходов.
4. **predict (ансамбль)** — сводит три независимых источника в финальную
   вероятность и считает value-перевес (`p·odds − 1`) против лучшего коэф.
5. **scanner** — связывает всё, отдаёт список вилок (сорт. по марже), шлёт
   уведомления в Telegram и логирует находки в Supabase.

## Как достигается точность прогноза

Точность даёт не один источник, а **ансамбль независимых сигналов** — их
смешение устойчивее и калиброваннее любого по отдельности:

| Источник | Модуль | Что даёт |
|---|---|---|
| Консенсус рынка | `core/devig.js` | «честные» вероятности без маржи БК, усреднённые по всем букмекерам — сильнейший базовый предиктор |
| Модель Пуассона | `core/poisson.js` | вероятности 1X2 и тоталов из силы атаки/обороны команд (средние голы по форме) |
| LLM-разбор | `core/motivation.js` | качественная поправка: мотивация, кому нужнее очки, вероятный исход |

`core/blend.js` смешивает их с весами (`ARB_W_*`), `core/predict.js` считает
финальную вероятность, value-перевес и **согласие источников** (доверие растёт,
когда сигналы сходятся). Каждый источник опционален: без ключей статистики/LLM
остаётся консенсус рынка — уже рабочий прогноз.

**Калибровка.** `store/grade.js` после матча сверяет прогноз с фактом и считает
**Brier score** и **log-loss** (пишутся в `arb_surebets`) — так видно реальную
точность и можно подбирать веса источников.

## Уведомления и лог

- **Telegram** (`notify/telegram.js`) — при вилке с маржой ≥ `ARB_NOTIFY_MARGIN`
  шлёт сообщение в чат/канал. Нужны `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`.
- **Supabase** (`store/supabase.js`) — пишет находки в таблицу `arb_surebets`
  (схема — `schema.sql`, применить в SQL editor). upsert по (event, market,
  line) — повторный скан не плодит дубли. Нужны `SUPABASE_URL` и
  `SUPABASE_SERVICE_ROLE_KEY`. Позже можно сверять ИИ-прогноз с результатом.

Оба слоя опциональны: без ключей сканер просто ищет и печатает вилки.

## Запуск

Node.js 18+.

```bash
cd arb-core
cp .env.example .env     # вписать ODDS_API_KEY (и по желанию API_FOOTBALL_KEY, ANTHROPIC_API_KEY)
npm run demo             # показать работу на встроенных данных (без ключей/сети)
npm run scan             # вилки (surebet) по ближайшим событиям (upcoming)
npm run scan soccer_epl  # конкретный вид спорта / лига
npm run value soccer_epl # value-ставки: где цена выгоднее честной вероятности рынка
npm run value soccer_epl -- --ai     # value + модель Пуассона и LLM (нужны ключи)
npm run value soccer_epl -- --sharp  # только value у резких контор (их не режут)
npm run value soccer_epl -- --hours=48  # только матчи в ближайшие 48ч (0 = без окна)
```

**Окно по времени.** По умолчанию сканируются матчи в ближайшие
`ARB_MAX_HOURS` часов (72). Ранние futures-линии (за недели до игры) сырые и
сильно двигаются — по ним не ставят, поэтому окно их отсекает. `--hours=N`
переопределяет, `--hours=0` снимает ограничение.

**Автогрейдинг (измерение точности).** Каждый `npm run value` пишет прогнозы в
локальный `predictions.jsonl`. После матчей `npm run grade` тянет результаты с
The Odds API (те же event_id) и считает **Brier**, **log-loss**, **hit rate**, а
по флагнутым value-ставкам — **ROI**. Это единственный способ узнать, реально ли
прогноз обыгрывает рынок, а не только выглядит убедительно.

```bash
npm run value upcoming   # копит прогнозы в predictions.jsonl
# ...после того как матчи сыграны (в пределах ARB_SCORES_DAYS суток):
npm run grade            # отчёт по точности; npm run grade soccer_epl — по одному спорту
```

Лог локальный (в `.gitignore`), облако не требуется. Запускай грейдинг регулярно:
`/scores` отдаёт результаты только за последние 1–3 суток.

Файл `.env` подхватывается автоматически. **Вилки vs value:** чистые вилки
(`scan`) редки и живут секунды; value-ставки (`value`) — где лучший коэффициент
выгоднее консенсуса рынка (плюсовое ожидание) — встречаются регулярно и есть
даже когда вилок нет.

**Резкость контор.** Консенсус взвешивается в пользу резких контор (Pinnacle,
Betfair, Smarkets, Matchbook) — их линия эталонная, прогноз точнее. Value у
софт-контор требует более высокого перевеса (`ARB_MIN_EDGE_SOFT`), чем у резких
(`ARB_MIN_EDGE`): софт-конторы раздают щедрые коэффициенты на аутсайдеров, но
лимитируют и банят выигрывающих. `--sharp` показывает только реально отыгрываемые
сигналы — у резких контор.

Без `ODDS_API_KEY` скан не запустится (нужны реальные коэффициенты). Без
`ANTHROPIC_API_KEY` вилки всё равно находятся — просто без ИИ-разбора.

## Тесты

```bash
npm test    # node --test — чистая математика вилок, нормализация, разбор ИИ
```

Тесты покрывают ядро без сети (детект вилок, раскладка ставок, нормализация
линий, парсинг ответа модели, расчёт value-перевеса).

## Структура

```
src/
  config.js              переменные окружения, наличие ключей
  index.js               CLI: скан и печать вилок
  sources/
    oddsApi.js           The Odds API — коэффициенты БК
    apiFootball.js       API-Football — форма, таблица (контекст для ИИ)
  core/
    normalize.js         линии БК → лучший коэф. на исход (h2h/totals/spreads)
    arbitrage.js         математика вилок и раскладки ставок (чистая)
    devig.js             снятие маржи БК + консенсус вероятностей рынка
    poisson.js           статистическая модель голов (1X2, тоталы)
    blend.js             ансамбль вероятностей + метрики (Brier, log-loss)
    motivation.js        ИИ-разбор матча (Claude)
    predict.js           сборка прогноза из источников + value-перевес
    scanner.js           оркестратор: коэф. → вилки → прогноз → уведомл./лог
  notify/
    telegram.js          уведомления о вилках в Telegram
  store/
    supabase.js          лог находок в Supabase (PostgREST)
    grade.js             грейдинг прогноза по факту (Brier/log-loss)
  util/
    http.js              fetch с ретраями и таймаутом
schema.sql               таблица arb_surebets для Supabase
test/                    node --test
```

## Дальше можно добавить

- Больше видов спорта и рынков (обе половины, точный счёт).
- Учёт лимитов/комиссий БК и «протухания» коэффициентов.
- Планировщик (cron) для периодического скана.
- Сверку ИИ-прогноза с фактическим результатом (точность модели).
